// Server-side, tool-calling AI shopping assistant.
// NOT implemented until Phase 13 — deliberately empty. When built, this
// package must only expose read-only, grounded product-search tools to
// the model; it must never grant raw SQL access or allow the model to
// state a price/stock/spec value that didn't come from a tool result.
export {};
